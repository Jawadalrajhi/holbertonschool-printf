.TH _PRINTF 3 "October 4, 2025"
.SH NAME
_printf \- print formatted output to stdout
.SH SYNOPSIS
int _printf(const char *format, ...);
.SH DESCRIPTION
The _printf function prints text to the standard output according to a format string.
It returns the number of characters printed, not including the terminating null byte.
The function currently supports the following conversion specifiers:
  %c : prints a single character
  %s : prints a string
  %% : prints a percent sign
  %d : prints a signed decimal integer
  %i : prints a signed decimal integer
.SH EXAMPLES
_printf("Hello %s\n", "World");
_printf("Character: %c\n", 'A');
_printf("Percent sign: %%\n");
_printf("Number: %d\n", 42);
_printf("Number: %i\n", -42);
.SH NOTES
Allowed functions: write, malloc, free, and the stdarg macros.
This man page describes everything _printf can do so far for Tasks 0, 1, 3, 4, 7, 14, and 15.

